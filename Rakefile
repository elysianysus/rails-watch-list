# Add your own Lists in files placed in lib/Lists ending in .rake,
# for example lib/Lists/capistrano.rake, and they will automatically be available to Rake.

require_relative "config/application"

Rails.application.load_Lists
